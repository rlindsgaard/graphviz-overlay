[tool.poetry]
name = "graphviz-overlay"
version = "0.1.0"
description = ""
authors = ["Ronni Elken Lindsgaard <rln@csis.dk>"]

[tool.poetry.dependencies]
python = "^3.10"
graphviz = "^0.19.1"

[tool.poetry.dev-dependencies]
pytest = "^6.2.5"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
